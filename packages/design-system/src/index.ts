/**
 * @kotn/design-system — cross-platform tokens + helpers.
 * Primitives live per-app (platform-native impl); this package is the shared
 * source of truth for tokens and the CSS generator.
 */
export * from './tokens/index';
export { formatMinorUnits } from './format';
