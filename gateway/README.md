# API Gateway — Go

Central orchestrator: auth, rate-limit, POS hardware-ID + merchant-credential validation, REST→gRPC routing, P2P shard rebalancing.

_No code yet — scaffolded during build window (Jul 3–12)._
