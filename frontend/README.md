# Frontend — Next.js

Consumer wallet dashboard + merchant POS simulator. Camera capture, light crop/resize, fires REST to the Go API Gateway.

_No code yet — scaffolded during build window (Jul 3–12)._
