# Copy to .env and adjust. NEVER commit .env (it is gitignored).

# --- Postgres ---
POSTGRES_USER=kotn
POSTGRES_PASSWORD=kotn
POSTGRES_DB=kotn
POSTGRES_PORT=5432

# --- Services ---
GATEWAY_HTTP_PORT=8080
WALLET_HTTP_PORT=8081

# Moka United runs as a deterministic mock until sandbox dealer creds land (ADR-0002).
MOKA_MOCK=true
# MOKA_DEALER_CODE=
# MOKA_USERNAME=
# MOKA_PASSWORD=
